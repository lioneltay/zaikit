services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "7302:5432"
    environment:
      POSTGRES_USER: zaikit
      POSTGRES_PASSWORD: zaikit
      POSTGRES_DB: zaikit
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
